# Demo Video Plan

1. Open the Operator Console locally.
2. Run `POST /api/fixtures/icon-search/executions` through the stable fixture replay path, or paste the fixture transcript into the console.
3. Show the transcript panel and normalized Browser Task Request.
4. Run the controlled icon-only toolbar task and show the timeline with visual grounding evidence references.
5. Run `checkout-stop` and show the Confirmation Gate prompt.
6. Cancel the confirmation and show the final trace export.
7. Close with the sanitized trace artifact and quick reminder that raw recordings and private traces stay local.
